import { PopMenuDirective } from './pop-menu.directive';

describe('PopMenuDirective', () => {
  it('should create an instance', () => {
    const directive = new PopMenuDirective();
    expect(directive).toBeTruthy();
  });
});
