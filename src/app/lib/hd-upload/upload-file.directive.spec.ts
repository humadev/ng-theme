import { UploadFileDirective } from './upload-file.directive';

describe('UploadFileDirective', () => {
  it('should create an instance', () => {
    const directive = new UploadFileDirective();
    expect(directive).toBeTruthy();
  });
});
